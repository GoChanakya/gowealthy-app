# upload-service
PORT=3001
GCP_PROJECT_ID=gowealthy-app
UPLOAD_BUCKET=document-ocr203
# Local only. Omit on Cloud Run (uses the service account's default credentials).
# GCS_KEY_FILE=./service-account-key.json
